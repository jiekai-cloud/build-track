name: Daily System Health Check 🏥

on:
  schedule:
    # 每天凌晨 03:00 (UTC時間 19:00) 自動執行
    - cron: '0 19 * * *'
  workflow_dispatch: # 允許手動點擊按鈕立即觸發

jobs:
  health-check:
    name: 深度系統檢查
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci
        
      - name: TypeScript Strict Check 🛡️
        # 如果檢查失敗，不要直接終止，繼續跑 Build 看是否致命
        continue-on-error: true
        run: npx tsc --noEmit
      
      - name: Production Build Simulation 🏗️
        run: npm run build
      
      - name: Report Status
        if: failure()
        run: |
          echo "❌ 系統檢查發現嚴重問題！請立即查看詳細日誌。"
          # 這裡未來可以串接 Slack 或 Line 通知
